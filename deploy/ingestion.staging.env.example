# Non-secret env vars for the ingestion staging service.
# Copy these into Cloud Run (Environment variables) for `profitscout-ingestion-staging`.
# Do NOT commit real secret values here; secrets come from Secret Manager bindings.

PS_BQ_PROJECT=your-staging-project
PS_BQ_DATASET_INGESTION=ps_ingest_staging
PS_GCS_BUCKET_INGESTION=profitscout-staging-data
PS_REGION=us-central1
PS_ENV=staging

# Optional knobs (safe defaults):
# PS_RATE_LIMIT_RPS=5
# PS_LOG_LEVEL=INFO
